{
  "name": "wasm-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "cmake --build build --parallel",
    "test": "npm run build && rm -f tests/machinecode/*.bin && rm -f tests/machinecode/*.asm && ASAN_OPTIONS=detect_leaks=0 ctest --test-dir build --output-on-failure --gtest_shuffle && npm run objdump",
    "objdump": "./objdump.sh",
    "json2gtest": "python3 test-generator/generate.py ./tests/assets/ ./tests/",
    "wasi": "./compile-wasi.sh",
    "coverage": "npm test && mkdir -p coverage && lcov -d build/ -c -o coverage/coverage.info && lcov -r coverage/coverage.info '*/build/*' '*/11/*' '*/extern/*' '*/tests/*' -o coverage/coverage.filtered.info && genhtml coverage/coverage.filtered.info --output-directory coverage/report && lcov --summary coverage/coverage.filtered.info",
    "linter": "find src -regex '.*\\.\\(cpp\\|hpp\\|c\\|h\\)' -not -path '**/kaitai/**' | xargs clang-format -style=file:.clang-format --Werror --dry-run",
    "kaitai": "kaitai-struct-compiler --target cpp_stl --cpp-standard 11 --outdir src/kaitai src/kaitai/webassembly.ksy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/henrythasler/wasm-playground.git"
  },
  "keywords": [
    "wasm"
  ],
  "author": "Henry Thasler",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/henrythasler/wasm-playground/issues"
  },
  "homepage": "https://github.com/henrythasler/wasm-playground#readme",
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
